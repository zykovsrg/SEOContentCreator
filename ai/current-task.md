# Current Task

Status: active

Allowed statuses: empty / active / review / blocked / done / paused

Stage: intake

Allowed stages: intake / spec / planning / implementation / review / task-finish

## Mode

implementation

## Goal

Сделать живой сбор семантики проходимым до конца на реальных данных. Две
подзадачи (первая уже выполнена):

1. [DONE] Исправлен `WordstatResponseParser.ParserError` — Wordstat при
   отсутствии данных по фразе возвращает `{}` без поля `results`, парсер
   считал это поломкой.
2. [расширение по запросу пользователя 2026-07-23] После фикса живой прогон
   дошёл до 201 из 1664 запросов и упёрся в лимит квоты Yandex Cloud
   (`quotaExceeded`). Нужно сократить количество запросов к Wordstat до
   не более 100 на один сбор с минимальными потерями качества: жёсткий
   потолок в `SemanticSeedPlan.seedPhrases()` с приоритизацией — сначала
   все чистые синонимы, затем комбинации с масками по кругу между
   синонимами, затем хвосты по кругу, дедупликация, обрезка на 100.
   Обоснование: каждый запрос к Wordstat и так возвращает до 200 связанных
   фраз (включая вопросные формы), а фильтр правил всё равно оставляет
   топ-100 по частотности, поэтому потеря качества минимальна.

Замечание: механизм resume/остановки работает правильно — оба живых прогона
корректно останавливались на ошибке, сохраняли прогресс и предлагали
«Продолжить сбор».

## Use Superpowers

yes

## Relevant files

SEOContentCreator/SEOContentCreator/Logic/WordstatResponseParser.swift
SEOContentCreator/SEOContentCreator/Logic/WordstatCloudClient.swift
SEOContentCreator/SEOContentCreator/Logic/WordstatLegacyClient.swift
SEOContentCreator/SEOContentCreator/Logic/WordstatCredentialStore.swift

## Done criteria

- Найдена корневая причина ParserError на реальном запросе к Wordstat. [DONE]
- `SemanticSeedPlan.seedPhrases()` никогда не возвращает больше 100 фраз;
  приоритет: чистые синонимы → маски по кругу между синонимами → хвосты по
  кругу; дедупликация сохраняется.
- Юнит-тесты покрывают лимит, порядок приоритизации и равномерность
  распределения масок между синонимами.
- Сбор семантики на реальной теме проходит от кнопки до сохранённого
  результата без ParserError и без исчерпания квоты (либо ошибка честно
  описывает причину на стороне Wordstat/аккаунта).

## Agent handoff

Last agent: Claude Code, 2026-07-23

What changed:

- Корневая причина найдена и подтверждена живым запросом к реальному Cloud
  API (curl, с ключом/folderId из Keychain пользователя, тот же payload, что
  и в приложении): для фразы `"мрт"` API вернул валидный `results: [...]`, но
  для заведомо бессмысленной фразы без данных — `HTTP 200` с телом `{}`,
  без поля `results` вообще (не `"results": []`).
- `WordstatResponseParser`/`Response.results` был обязательным полем без
  дефолта, поэтому отсутствие ключа декодировалось как `DecodingError`,
  превращалось в `ParserError.badResponse` — то есть частый штатный случай
  «у фразы нет данных» ошибочно считался поломкой ответа.
- Это критично взаимодействовало с сегодняшним же изменением
  (`SemanticCollectionRunner` теперь останавливает весь прогон на первой
  ошибке Wordstat) — почти любой реальный прогон с тысячами seed-фраз
  падал на первой же фразе без данных.
- Исправлено: `Response` теперь декодирует `results` через
  `decodeIfPresent(...) ?? []`. Добавлен тест
  `treatsEmptyObjectAsNoPhrases()` в `WordstatResponseParserTests.swift`,
  воспроизводящий именно тело `{}`. Все тесты парсера (5 шт.) и
  соответствующие тесты раннера прошли.
- Собрана и установлена Release-сборка в `/Applications`; старая копия
  сохранена как бэкап. База данных (7 тем) на месте.

Update (same day, seed cap):

- Первый живой прогон после фикса парсера подтвердил фикс: дошёл до 201 из
  1664 запросов и остановился уже на честном `quotaExceeded` (лимит Yandex
  Cloud), прогресс сохранён чекпоинтом.
- По решению пользователя добавлен жёсткий потолок:
  `SemanticSeedPlan.maxSeedPhrases = 100`. Приоритизация в `seedPhrases()`:
  сначала чистые синонимы, затем маски по кругу между синонимами, затем
  хвосты по кругу; дедупликация сохранена. 3 новых теста + все старые тесты
  парсера плана и выборочные тесты раннера зелёные.
- Собрана Release-сборка, установлена в `/Applications` (бэкап прежней копии
  в `/private/tmp/`), НО: приложение отказалось закрыться через AppleScript
  («User cancelled»), старый процесс продолжал работать на старом коде.
  Пользователь должен сам перезапустить приложение, чтобы новая сборка
  вступила в силу.

Open risks:

- У темы «Фаст Форвард» остался чекпоинт со старым планом из 1664 фраз
  (прогресс 201). Чтобы новый лимит 100 подействовал, пользователь должен
  нажать «Сбросить» и начать сбор заново — «Продолжить сбор» продолжит
  старый план из 1664.
- Квота Wordstat, возможно, ещё не восстановилась (лимит, вероятно,
  суточный) — новый сбор может сразу упереться в quotaExceeded до
  восстановления квоты.
- Ключ Wordstat, раскрытый в чате в предыдущей сессии, всё ещё не заменён
  (перенесённый риск, пользователь оставил открытым).

Next agent should check:

1. Убедиться, что пользователь перезапустил приложение (новая сборка с
   лимитом 100).
2. Пользователь нажимает «Сбросить» для темы и запускает новый сбор — план
   должен быть ≤100 запросов, сбор должен пройти до конца (возможно, после
   восстановления квоты).
3. Если всё ок — предложить `task-finish`.
