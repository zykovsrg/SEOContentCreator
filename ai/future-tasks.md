# Future Tasks

Use this file for ideas and future implementation tasks that are not part of the current task scope.

This is a backlog, not active work.

## Rules

- Do not implement these tasks unless the user explicitly promotes one to the current task.
- Do not use this file for paused active work. Use `ai/paused-tasks.md` for interrupted unfinished tasks.
- Do not use this file for completed change history. Use `ai/changelog.md` for what changed.
- Do not use this file for durable architecture or product decisions. Use `ai/decisions.md` for rules future agents must not break.
- Keep entries short, actionable, and linked to the context where they appeared.
- If a future task becomes active, copy it into `ai/current-task.md` and mark the original entry as `promoted`.

## Statuses

```text
idea / ready / blocked / promoted / done / dropped
```

## Template

### FT-YYYYMMDD-001 — Task title

Status: idea

Priority: low / medium / high

Source: where the idea appeared

Created: YYYY-MM-DD

Context:

Short context: why this may be useful later.

Proposed task:

What should be implemented later.

Acceptance criteria:

- How to know this future task is done.

Promotion notes:

What to check before moving this task to `ai/current-task.md`.

## Future tasks

### FT-20260703-006 — Ручная проверка 4 доработок редактора замечаний/изображений/ручной правки

Status: idea

Priority: high

Source: закрытие задачи «4 доработки редактора» 2026-07-03 — пользователь явно попросил закрыть без ручной проверки в приложении

Created: 2026-07-03

Context:

Реализованы 4 изменения (см. `ai/changelog.md` за 2026-07-03, запись «Четыре доработки редактора замечаний, изображений и ручной правки»): фикс бага «Удалить» + комментарий-доработка замечания, центрирование скролла в «Подсказках»/панели замечаний/«Быстрой проверке», `PipelineStage.images` как отдельный этап после «Финальной вычитки», кастомный `MarkdownTextEditor` с хоткеями в `ManualEditSheet`. Сборка (`xcodebuild build-for-testing`) и юнит-тесты новой логики зелёные, но ни разу не проверялось в живом запущенном приложении.

Proposed task:

1. Открыть тему с готовым текстом, запустить проверочный этап (SEO/фактчекинг/финальная вычитка), убедиться, что удаление фрагмента реально стирает текст (а не пишет слово «Удалить»), и что кнопка «Доработать» с комментарием реально перегенерирует правку (нужен настроенный ключ OpenAI).
2. В «Подсказках», на панели замечаний рабочего экрана и в «Быстрой проверке» кликнуть по нескольким пунктам списка — убедиться, что текст скроллится и центрируется на нужном абзаце.
3. Проверить, что «Изображения» в панели этапов и в тулбаре недоступны/показывают сообщение, пока не принята версия «Финальной вычитки», и становятся доступны после.
4. Открыть «Ручная правка», проверить Cmd+B, Cmd+I, Cmd+Option+1/2/3 — убедиться, что хоткеи не перехватываются системными шорткатами (переключение вкладок окна и т.п.), и что окно реально resizable.

Acceptance criteria:

- Все 4 пункта подтверждены (или для каждого расхождения заведён баг/новая future task).
- Результат проверки записан в `ai/changelog.md`.

Promotion notes:

Если хоткеи Cmd+Option+1/2/3 не срабатывают — проверить `MarkdownEditorTextView.keyDown` в `MarkdownTextEditor.swift` и не перехватывает ли их системное меню/NSWindow tabbing раньше, чем событие доходит до `keyDown`.

---

### FT-20260703-005 — Новый этап «Правки по комментариям врачей»

Status: idea

Priority: medium

Source: пожелание пользователя 2026-07-03

Created: 2026-07-03

Context:

Пользователь получает комментарии от врачей по готовому тексту: записывает встречу, просит стороннего ИИ проанализировать разговор и составить правки в определённом JSON-формате. Сейчас вставить эти правки в приложение и автоматически применить их к тексту негде.

Proposed task:

Добавить новый этап пайплайна «Текст по комментариям», идущий после финальной вычитки и генерации изображений. Пользователь вставляет JSON с правками от внешнего ИИ-анализа встречи, приложение на их основе запускает генерацию, которая вносит замечания врача в текущую версию текста (по аналогии с существующим механизмом применения замечаний/remarks).

Acceptance criteria:

- Определён и задокументирован ожидаемый JSON-формат правок от врача.
- Новый этап встроен в общий порядок этапов (после финальной вычитки и генерации изображений).
- Вставленные правки корректно применяются к тексту через генерацию, как обычные remarks.

Promotion notes:

Нужно сначала согласовать точный JSON-формат правок (что присылает внешний ИИ) и то, чем этот этап отличается от уже существующего механизма замечаний/RemarkApplier — возможно, это переиспользование существующей механики с новым источником входных данных, а не полностью новый этап.

---

### FT-20260703-004 — Переработка промтов для генерации картинок в JSON-формат

Status: idea

Priority: low

Source: пожелание пользователя 2026-07-03

Created: 2026-07-03

Context:

Пользователь хочет генерировать промты для изображений в структурированном JSON-формате (поля style/scene/subject/lighting/details/camera/mood/aspect_ratio) вместо текущего свободного текстового промта, с примером в духе «underground fashion photography» с одеждой из подручных материалов.

Proposed task:

Изменить формирование промта для генерации изображений так, чтобы он собирался в JSON-структуру с полями style, scene, subject, lighting (type/source), details, camera, mood, aspect_ratio — конкретный набор полей и шаблон согласовать с пользователем перед реализацией.

Acceptance criteria:

- Промт для изображений формируется в согласованном JSON-формате.
- Пользователь может настраивать/видеть значения по каждому полю.

Promotion notes:

Нужно уточнить у пользователя точный набор полей и то, как поле "subject" (например, "описание пакета") должно наполняться — вручную, из текста статьи, или через отдельный поиск.

---

### FT-20260703-003 — Новый этап «Анализ и обучение» промптов

Status: idea

Priority: medium

Source: пожелание пользователя 2026-07-03

Created: 2026-07-03

Context:

Пользователь хочет быстрее настраивать систему промтов под нужный результат текста. Идея: версии сгенерированного текста и текущая система промтов отправляются отдельному ИИ-агенту на анализ; агент возвращает рекомендации по улучшению промтов в формате [проблема или объединённая группа проблем] → [текст промта]/[предложение добавить новый текст] → [улучшение].

Proposed task:

Добавить новый этап пайплайна «Анализ и обучение»: отправляет ИИ-агенту версии текста темы вместе с текущими промтами этапов, получает и показывает пользователю структурированные рекомендации по правкам промтов в указанном формате (без автоматического применения — пользователь сам решает, какие рекомендации внести).

Acceptance criteria:

- Пользователь может запустить анализ по теме и получить список рекомендаций в формате [проблема] → [текущий промт/место] → [предлагаемое улучшение].
- Рекомендации не применяются автоматически — только показываются для ручного решения.

Promotion notes:

Нужно уточнить у пользователя: какие именно версии текста и какие промты отправлять агенту (все этапы темы или выбранные), а также где именно в интерфейсе размещать этот этап и куда сохранять историю рекомендаций.

---

### FT-20260703-002 — Ручная проверка скорости генерации после фикса MarkdownBlocksView

Status: idea

Priority: medium

Source: закрытие задачи "тормозит генерация текста" 2026-07-03 — пользователь попросил закрыть до ручной проверки

Created: 2026-07-03

Context:

Найден и исправлен root cause торможения UI при длинной генерации: `MarkdownBlocksView` перепарсивал весь текст текущей статьи на каждый стримингованный токен (см. `ai/changelog.md` за 2026-07-03). Исправление собрано (`build-for-testing` и Release build — оба успешны) и установлено в `/Applications/SEOContentCreator.app`, но реальный сквозной прогон длинной генерации на статье с большим объёмом уже готового текста в этой сессии не проводился.

Proposed task:

Открыть тему с длинной существующей статьёй (в идеале — несколько тысяч слов), запустить генерацию любого авторского этапа и на глаз оценить, что скорость появления токенов не деградирует по мере роста уже готового текста, как это было раньше.

Acceptance criteria:

- Подтверждено (или опровергнуто) на реальных данных, что торможение больше не воспроизводится.

Promotion notes:

Если торможение всё ещё воспроизводится — вернуться к `ai/skills/systematic-debugging` (Superpowers), т.к. первая гипотеза могла объяснять лишь часть проблемы.

---

### FT-20260703-001 — Рендерить Markdown и в VersionCompareView

Status: promoted

Promoted on 2026-07-03 into `ai/current-task.md`, branch `main` (автономный запуск, 2-я из 6 задач сессии). См. `ai/changelog.md`.

Priority: low

Source: реализация FT-20260702-018 (2026-07-02/03) — тот же сырой-Markdown недостаток замечен в соседнем экране

Created: 2026-07-03

Context:

`VersionCompareView.swift` сравнивает две версии текста через `ParagraphDiff` (уже показывает добавленное/удалённое корректно), но выводит абзацы обычным `Text(line.text)` — заголовки видны как «## Заголовок» с решётками, как раньше было в `SideBySideView`.

Proposed task:

Заменить `Text(line.text)` в `VersionCompareView.column` на `MarkdownBlocksView(text: line.text)` (уже создан в рамках FT-20260702-018) — сохранив существующие `.strikethrough`/фон по `line.kind`.

Acceptance criteria:

- Заголовки/списки в сравнении версий отображаются отформатированными, как в `SideBySideView`.

Promotion notes:

Небольшое, изолированное изменение — переиспользует уже существующий компонент.

---

### FT-20260702-001 — Ручная проверка семантического AI-агента в запущенном приложении

Status: idea

Priority: high

Source: закрытие задачи «Семантический AI-агент» 2026-07-02 (FT-20260623-001); пользователь явно попросил вынести проверку механики в отдельную задачу вместо проверки сразу

Created: 2026-07-02

Context:

Реализация семантического AI-агента (модель `SemanticKeyword`, экран `SemanticAgentSheet`, таблица решений в `SemanticsEditorSheet`, индекс страниц сайта `PublishedSitePage`) полностью покрыта автоматическими тестами (`xcodebuild test` — все зелёные), но ни разу не проверялась вручную в запущенном приложении.

Proposed task:

Открыть приложение и проверить весь путь вручную:
1. Открыть существующую тему со старой семантикой (`Topic.semantics`) — убедиться, что старые запросы появляются в таблице как «принятые».
2. Открыть «Сбор агентом», сгенерировать тестовые запросы, запустить анализ через OpenAI (нужен настроенный ключ) — убедиться, что приходят рекомендации include/exclude с объяснением.
3. Сохранить результаты агента — проверить, что они попадают в таблицу как «ожидают решения», без дублей с уже существующими запросами.
4. Принять/отклонить/сделать обязательными несколько строк — проверить, что `{{семантика}}` в промте следующего этапа содержит только принятые и обязательные.
5. Нажать «Обновить страницы сайта» при доступном и при недоступном hadassah.moscow — убедиться, что при ошибке старый индекс не пропадает.

Acceptance criteria:

- Все 5 пунктов проверены вручную на реальном приложении с реальными данными темы.
- Найденные расхождения с ожидаемым поведением зафиксированы как баги или новые future tasks.
- Результат проверки записан в `ai/changelog.md`.

Promotion notes:

Требует настроенного ключа OpenAI (Keychain) для шага 2. Если каннибализация не находится на реальных данных сайта — проверить, что индекс `PublishedSitePage` действительно наполнен (шаг 5 перед шагом 2).

---

### FT-20260702-002 — Дублирование стриминга в StageExecutor

Status: idea

Priority: low

Source: аудит приложения 2026-07-02 (код, п. 1.1)

Created: 2026-07-02

Context:

`execute`, `executeSandbox` и `executeQuickCheck` в `Logic/StageExecutor.swift` (строки 46–270) дублируют один и тот же цикл стриминга, обработку обрыва по токенам и текст предупреждения об обрыве (трижды).

Proposed task:

Вынести общий цикл стриминга (сбор токенов, обработка `finish reason`, формирование предупреждения) в один приватный метод, из которого вызывать все три публичных метода.

Acceptance criteria:

- Текст предупреждения об обрыве существует в коде один раз.
- Поведение трёх методов не меняется (проверить существующими тестами).

Promotion notes:

Чисто рефакторинг — не смешивать с багфиксами. Перед стартом прогнать все тесты StageExecutor как baseline.

---

### FT-20260702-003 — Отмена генерации: метод в StageExecutor + кнопка «Стоп» в интерфейсе

Status: promoted

Promoted on 2026-07-03 into `ai/current-task.md`, branch `main` (автономный запуск, 1-я из 6 задач сессии). Реализовано только для `execute()`/`TopicWorkspaceView` (как и было предложено) — sandbox/quick-check/fragment-editor остались без кнопки «Стоп». См. `ai/changelog.md`.

Priority: high

Source: аудит приложения 2026-07-02 (код, п. 1.2; UX, п. 3.1)

Created: 2026-07-02

Context:

У `StageExecutor` (`Logic/StageExecutor.swift`) нет метода отмены, а `Task` в `Views/TopicWorkspaceView.swift:202` нигде не сохраняется — если генерация длится долго, остановить её нельзя.

Proposed task:

Добавить в StageExecutor хранение текущего `Task` и метод `cancel()`, отменяющий стрим. В `TopicWorkspaceView` добавить кнопку «Стоп» рядом с «Запустить этап», активную только во время `isRunning`.

Acceptance criteria:

- Нажатие «Стоп» останавливает запрос к OpenAI и возвращает интерфейс в обычное состояние.
- Незавершённый `GenerationJob` помечается как отменённый/ошибка, а не остаётся вечно «running».

Promotion notes:

Затрагивает все три сценария запуска (execute/executeSandbox/executeQuickCheck) — учесть при оценке объёма.

---

### FT-20260702-004 — Показывать точную причину ошибки OpenAI из тела ответа

Status: promoted

Promoted on 2026-07-03 into `ai/current-task.md`, branch `main` (автономный запуск, 3-я из 6 задач сессии). Также применено к `ImageClient.swift` (тот же `OpenAIClient.OpenAIError.http`, тело ответа уже было в руках) — не только к чат-этапам, как формулировалось изначально. См. `ai/changelog.md`.

Priority: medium

Source: аудит приложения 2026-07-02 (код, п. 1.3)

Created: 2026-07-02

Context:

В `Logic/OpenAIClient.swift:88-95` при HTTP-ошибке тело ответа не читается, хотя там OpenAI обычно пишет точную причину (например, «модель не существует»). Пользователь видит только общий код ошибки.

Proposed task:

Прочитать тело ответа при ошибочном статусе и включить сообщение OpenAI в `OpenAIError`, если оно есть.

Acceptance criteria:

- При HTTP 400/404/etc. пользователь видит причину из ответа OpenAI, а не только код статуса.

Promotion notes:

Не менять поведение для 401/429 — там сообщения уже осмысленные и специфичные.

---

### FT-20260702-005 — Сохранять токены и стоимость запуска в GenerationJob

Status: promoted

Promoted on 2026-07-03 into `ai/current-task.md`, branch `main` (автономный запуск, 4-я из 6 задач сессии). Реализованы оба пункта 1–2; пункт 3 (FT-20260623-006) реализуется следом в этой же сессии. Также добавлено сохранение токенов в `FragmentEditor.swift` (свой `GenerationJob`, тот же паттерн) — не только в `StageExecutor`. Схема `GenerationJob` расширена двумя опциональными полями — риск и решение см. `ai/decisions.md`. См. `ai/changelog.md`.

Priority: medium

Source: аудит приложения 2026-07-02 (код, п. 1.4)

Created: 2026-07-02

Context:

`Models/GenerationJob.swift` не хранит количество токенов; запрос в `Logic/OpenAIClient.swift` не просит usage-статистику (`stream_options.include_usage`). Это отменяет предпосылку задачи FT-20260623-006 «Стоимость по теме», которая ошибочно предполагала, что данные о токенах уже есть.

Proposed task:

1. Добавить `stream_options: {"include_usage": true}` в запрос к OpenAI и распарсить usage-событие в `OpenAILineParser`.
2. Добавить поля `promptTokens`/`completionTokens` (или общую стоимость) в `GenerationJob`.
3. После этого можно будет реализовать FT-20260623-006 корректно.

Acceptance criteria:

- После запуска этапа в GenerationJob видно количество токенов запроса и ответа.

Promotion notes:

Изменение SwiftData-схемы (новые поля) — согласовать миграцию перед стартом. Делать до FT-20260623-006, не одновременно.

---

### FT-20260702-006 — Разобраться с неиспользуемыми полями и сущностями

Status: idea

Priority: low

Source: аудит приложения 2026-07-02 (код, п. 1.5)

Created: 2026-07-02

Context:

Метки версий `manualEdit` и `importFromDocs` (`Models/ArticleVersion.swift:14`) нигде не создаются. `StageTemplate.modelName` хранится, но везде перекрывается глобальной моделью из Настроек, а в редакторе шаблонов показывается только для чтения (`Views/Templates/TemplateEditorView.swift:33`).

Proposed task:

Для каждого случая решить: либо это задел на будущую функциональность (тогда пометить TODO/комментарием почему), либо мёртвый код (тогда убрать поле/метку).

Acceptance criteria:

- По каждой из трёх находок принято и зафиксировано решение (оставить с пометкой или удалить).

Promotion notes:

Низкий риск, но `modelName` в StageTemplate — решение связано с UX-вопросом «нужна ли переопределяемая модель на шаблон» — уточнить у пользователя перед стартом.

---

### FT-20260702-007 — Передать Title/Description/H1 в проверку SEO и при публикации

Status: promoted

Promoted on 2026-07-02 into `ai/current-task.md`, branch `main` (автономный запуск, реализация всех 6 отобранных задач подряд).

Priority: high

Source: аудит приложения 2026-07-02 (логика/промты, п. 2.1, 2.2)

Created: 2026-07-02

Context:

Промт этапа «Проверка SEO» (`Logic/StageTemplateDefaults.swift:101-120`) просит проверить категорию «Title/Description», но в `Logic/PromptBuilder.swift` нет переменных для H1/seoTitle/seoDescription — в промт попадает только тело статьи. Та же проблема при публикации: `Logic/ArticlePublisher.swift:48` отправляет в Google Docs только `version.text`, без Title/Description/H1.

Proposed task:

1. Добавить в PromptBuilder переменные с текущими H1/seoTitle/seoDescription версии и передавать их в промт проверки SEO.
2. Уточнить у пользователя, нужно ли включать Title/Description/H1 в публикуемый Google Doc — реализовать по решению.

Acceptance criteria:

- Проверка SEO реально видит и оценивает Title/Description, а не только тело текста.
- Решение по публикации Title/Description зафиксировано и реализовано (или осознанно отклонено).

Promotion notes:

Пункт про публикацию — сначала спросить пользователя, это не однозначный баг, а вопрос требований.

---

### FT-20260702-008 — Блокировать запуск проверочных этапов на пустом тексте

Status: promoted

Promoted on 2026-07-03 into `ai/current-task.md`, branch `main` (автономный запуск, 5-я из 6 задач сессии). См. `ai/changelog.md`.

Priority: high

Source: аудит приложения 2026-07-02 (логика/промты, п. 2.3)

Created: 2026-07-02

Context:

`Logic/StageRunGuard.swift` проверяет только этап «Черновик». Проверочные этапы (SEO, фактчекинг, финальная вычитка) можно запустить у темы без текста — запрос уйдёт в OpenAI впустую.

Proposed task:

Расширить `StageRunGuard.messagePreventingRun` так, чтобы проверочные этапы (`stage.kind == .checking`) требовали непустой `topic.currentVersion?.text`.

Acceptance criteria:

- Запуск проверочного этапа без текущей версии текста блокируется понятным сообщением, как это уже сделано для черновика.

Promotion notes:

Небольшое изменение, низкий риск — чистая логика без изменения схемы.

---

### FT-20260702-009 — Передавать семантику в этап «Структура»

Status: idea

Priority: low

Source: аудит приложения 2026-07-02 (логика/промты, п. 2.4)

Created: 2026-07-02

Context:

Промт этапа «Структура» (`Logic/StageTemplateDefaults.swift:17-33`) не содержит `{{семантика}}`, хотя собранные ключевые запросы — источник для проектирования разделов.

Proposed task:

Добавить `{{семантика}}` в стандартный промт этапа «Структура» (с пояснением, что запросы — ориентир, а не список для расстановки).

Acceptance criteria:

- В промте структуры доступна переменная `{{семантика}}` и она заполнена принятыми запросами.

Promotion notes:

Уточнить у пользователя: возможно, семантика намеренно скрыта от структуры, чтобы не влиять на редакционную логику разделов — проверить это предположение перед реализацией.

---

### FT-20260702-010 — Снизить temperature для проверочных этапов

Status: promoted

Promoted on 2026-07-02 into `ai/current-task.md`, branch `main` (автономный запуск).

Priority: medium

Source: аудит приложения 2026-07-02 (логика/промты, п. 2.5)

Created: 2026-07-02

Context:

Все стандартные шаблоны получают temperature 0.6 (`Logic/StageTemplateDefaults.swift:7`), включая проверочные этапы (SEO, фактчекинг, вычитка), где нужен более предсказуемый, менее «творческий» ответ и стабильный JSON.

Proposed task:

Установить более низкую temperature (например, 0.2–0.3) по умолчанию для этапов с `stage.kind == .checking` в `StageTemplateContent`.

Acceptance criteria:

- У проверочных этапов temperature по умолчанию ниже, чем у авторских.

Promotion notes:

Затрагивает только значения по умолчанию для новых шаблонов — не трогает уже сохранённые пользовательские StageTemplate.

---

### FT-20260702-011 — Сохранять результаты проверок (замечания) в базе

Status: idea

Priority: medium

Source: аудит приложения 2026-07-02 (логика/промты, п. 2.6)

Created: 2026-07-02

Context:

Замечания живут только в памяти (`StageExecutor.remarks`, `Logic/StageExecutor.swift:21-22`). Если закрыть приложение до нажатия «Готово», платная проверка пропадает без следа — в логе остаётся только «успех/ошибка».

Proposed task:

Персистить список замечаний (например, как JSON в `GenerationJob` или отдельную сущность), чтобы их можно было посмотреть и применить позже, даже после перезапуска приложения.

Acceptance criteria:

- После перезапуска приложения замечания последней проверки не теряются.

Promotion notes:

Изменение SwiftData-схемы — сначала объяснить пользователю риск и путь миграции.

---

### FT-20260702-012 — Учитывать позицию цитаты при замене в RemarkApplier

Status: promoted

Promoted on 2026-07-02 into `ai/current-task.md`, branch `main` (автономный запуск). Реализовано уже конкретнее исходной формулировки — см. `ai/changelog.md` за эту дату: вместо изучения передачи позиции от модели, найдена и исправлена реальная ошибка (более поздняя правка могла случайно попасть в текст, только что вставленный предыдущей правкой).

Priority: low

Source: аудит приложения 2026-07-02 (логика/промты, п. 2.7)

Created: 2026-07-02

Context:

`Logic/RemarkApplier.swift:9` заменяет только первое вхождение цитаты в тексте. Если короткая фраза встречается в статье дважды, правка может попасть не туда, где её имела в виду модель.

Proposed task:

Изучить, может ли модель возвращать позицию/контекст цитаты (например, окружающие слова) и использовать это для более точного поиска места замены.

Acceptance criteria:

- Для текстов с повторяющимися цитатами правка применяется к ожидаемому вхождению чаще, чем сейчас (нужен тестовый сценарий с дублирующейся фразой).

Promotion notes:

Риск для медицинских текстов небольшой, но реальный — не топ-приоритет, но не потерять из виду.

---

### FT-20260702-013 — Обратная связь при проверке: «Замечаний нет» + скрыть сырой JSON во время генерации

Status: idea

Priority: medium

Source: аудит приложения 2026-07-02 (UX, п. 3.2, 3.3)

Created: 2026-07-02

Context:

Если проверка вернула ноль замечаний, экран молча возвращается в обычный вид (`Views/TopicWorkspaceView.swift:135-137`) — непонятно, сработала ли проверка. Пока проверочный этап выполняется, в правой колонке виден поток сырого JSON-ответа (`Views/TopicWorkspaceView.swift:124-127`), что выглядит как поломка.

Proposed task:

1. При нулевом количестве замечаний показать явное сообщение «Проверка пройдена, замечаний нет».
2. Во время выполнения проверочного этапа показывать индикатор «Идёт проверка…» вместо сырого потока JSON.

Acceptance criteria:

- После проверки без замечаний пользователь видит явное подтверждение.
- Во время проверки не виден сырой JSON.

Promotion notes:

Чисто UI-изменение, без изменений схемы.

---

### FT-20260702-014 — Ручная правка всего текста статьи

Status: promoted

Promoted on 2026-07-02 into `ai/current-task.md`, branch `main` (автономный запуск).

Priority: high

Source: аудит приложения 2026-07-02 (UX, п. 3.4)

Created: 2026-07-02

Context:

Инвариант проекта (`ai/project-context.md`): «Ручная правка — сквозная, доступна на любом этапе». Но в интерфейсе можно править только структуру и фрагмент через ИИ — простого «открыть текст целиком и поправить руками» нет, метка версии `manualEdit` (`Models/ArticleVersion.swift`) нигде не создаётся.

Proposed task:

Добавить возможность открыть текущую версию текста целиком в редактируемом виде и сохранить правки как новую версию с меткой `manualEdit`.

Acceptance criteria:

- Пользователь может вручную отредактировать весь текст текущей версии на любом этапе.
- Сохранённая правка создаёт версию с источником `manualEdit` и становится текущей.

Promotion notes:

Согласовать с пользователем: где размещать эту функцию в интерфейсе (отдельный лист/кнопка рядом с версиями).

---

### FT-20260702-015 — Улучшить Контент-план для большого числа тем

Status: idea

Priority: medium

Source: аудит приложения 2026-07-02 (UX, п. 3.5)

Created: 2026-07-02

Context:

`Views/ContentPlanView.swift`: таблица из 4 колонок без фильтра по статусу, без сортировки по клику на колонку, без даты обновления и прогресса по этапам. При росте числа тем ориентироваться станет тяжело.

Proposed task:

Добавить фильтр по статусу темы (Идея/Готова/Опубликовано), сортировку по колонкам, колонку даты обновления.

Acceptance criteria:

- В Контент-плане можно отфильтровать темы по статусу и отсортировать по любой колонке.

Promotion notes:

Реализовывать по одному пункту, не смешивать с FT-20260702-016 (прогресс по этапам) — разные компоненты интерфейса.

---

### FT-20260702-016 — Показывать прогресс по этапам в панели этапов

Status: promoted

Promoted on 2026-07-02 into `ai/current-task.md`, branch `main` (автономный запуск).

Priority: medium

Source: аудит приложения 2026-07-02 (UX, п. 3.6)

Created: 2026-07-02

Context:

`Views/StageBarView.swift` подсвечивает только выбранный этап — не видно, какие этапы у темы уже пройдены.

Proposed task:

Добавить визуальный индикатор (например, галочку или другой цвет) для этапов, по которым уже есть принятая версия у темы.

Acceptance criteria:

- В панели этапов видно, какие этапы уже пройдены, а какие ещё нет.

Promotion notes:

Низкий риск, чтение существующих данных (versions по stage), изменений схемы не требует.

---

### FT-20260702-017 — Решить вопрос каскадного удаления версий при удалении темы

Status: idea

Priority: medium

Source: аудит приложения 2026-07-02 (UX, п. 3.7)

Created: 2026-07-02

Context:

Удаление темы (`Views/ContentPlanView.swift`) каскадно удаляет все версии текста (`Models/Topic.swift:25-26` — `deleteRule: .cascade`). Формально это расходится с инвариантом «версии не удаляются, только архивируются» — хотя для удаления *всей темы целиком* это может быть осознанным решением.

Proposed task:

Уточнить у пользователя: удаление темы должно необратимо стирать версии (как сейчас) или тема должна архивироваться вместо физического удаления? Реализовать по решению.

Acceptance criteria:

- Решение по поведению удаления темы явно зафиксировано в `ai/decisions.md`.

Promotion notes:

Не менять поведение удаления без обсуждения — это вопрос данных, которые нельзя восстановить.

---

### FT-20260702-018 — Рендерить Markdown в SideBySideView + показывать удалённые абзацы в дифе

Status: promoted

Promoted on 2026-07-02 into `ai/current-task.md`, branch `main` (автономный запуск, последняя из 6 задач сессии).

Priority: high

Source: аудит приложения 2026-07-02 (дизайн, п. 4.1, 4.2)

Created: 2026-07-02

Context:

Статьи пишутся в Markdown, но `Views/SideBySideView.swift` выводит их обычным `Text` — заголовки видны как `## Заголовок` с решётками. Кроме того, функция `ParagraphDiff.oldSide` (для показа удалённых абзацев в левой колонке) уже написана, но нигде не используется — левая колонка не показывает, что модель выбросила.

Proposed task:

1. Добавить рендер Markdown (хотя бы заголовки, списки, жирный/курсив) в обеих колонках SideBySideView.
2. Использовать `ParagraphDiff.oldSide` в левой колонке, чтобы подсвечивать удалённые абзацы (аналогично добавленным справа).

Acceptance criteria:

- Заголовки и списки в статье отображаются отформатированными, а не как сырой Markdown-текст.
- В левой колонке видно, какие абзацы были удалены при генерации новой версии.

Promotion notes:

Самая заметная ежедневная проблема по итогам аудита — приоритет высокий. Рендер Markdown может быть сторонним пакетом или простым парсером под нужды проекта — выбрать подход перед стартом.

---

### FT-20260702-019 — Контраст выбранного этапа + визуал карточки замечания

Status: idea

Priority: low

Source: аудит приложения 2026-07-02 (дизайн, п. 4.3, 4.4)

Created: 2026-07-02

Context:

Выбранная капсула этапа отличается от невыбранной еле заметно (`accentColor.opacity(0.2)` против `gray.opacity(0.1)`, `Views/StageBarView.swift:22`). Карточка замечания использует захардкоженный синий цвет для любой категории и показывает «было/станет» плоским текстом без подсветки изменений (`Views/RemarksPanelView.swift:30-35`).

Proposed task:

1. Увеличить контраст выделения выбранного этапа.
2. Раскрасить категории замечаний по смыслу (ошибка/стиль/факт) и подсветить изменённые слова в «было → станет».

Acceptance criteria:

- Выбранный этап визуально однозначно отличим от остальных.
- Категории замечаний различимы по цвету, различия в тексте подсвечены.

Promotion notes:

Чисто декоративные изменения — можно делать через ui-review без write-tests, если поведение не меняется.

---

### FT-20260701-001 — Live-smoke перезаписи Google Docs на тестовом документе

Status: idea

Priority: medium

Source: закрытие задачи укрепления логики 2026-07-01

Created: 2026-07-01

Context:

Безопасная перезапись Google Docs покрыта unit-тестами и сборкой, но в этой сессии не прогонялась против реального Google Docs API.

Proposed task:

Проверить перезапись на одноразовом тестовом Google Doc: создать публикацию, перезаписать её, имитировать ошибочный сценарий на безопасном документе и убедиться, что опубликованный документ не остаётся пустым.

Acceptance criteria:

- Перезапись реального тестового Google Doc проходит успешно.
- При ошибке тестовый документ не остаётся пустым.
- Результат проверки записан в changelog.

Promotion notes:

Использовать только одноразовый документ, не рабочие клиентские материалы.

---

### FT-20260623-006 — Стоимость по теме в Контент-плане

Status: promoted

Promoted on 2026-07-03 into `ai/current-task.md`, branch `main` (автономный запуск, 6-я и последняя из 6 задач сессии). Столбец назван «Токены», а не «Стоимость» — в приложении нет прайсинга/конвертации в деньги, показывать сырую сумму токенов под лейблом «Стоимость» было бы вводящим в заблуждение (решение принято автономно). См. `ai/changelog.md`.

Priority: low

Source: анализ спеки 2026-06-23 (раздел 4.1)

Created: 2026-06-23

Context:

Спека: в Контент-плане колонка «Стоимость по теме» (сумма токенов всех `GenerationJob` для темы). Данные уже хранятся в `GenerationJob`.

Proposed task:

1. Добавить вычисляемое свойство `Topic.totalTokenCost` — сумма токенов по всем `GenerationJob`.
2. Отобразить в строке темы в Контент-плане (опционально: только в расширенном режиме).

Acceptance criteria:

- Стоимость обновляется после каждого запуска этапа.

Promotion notes:

- Низкий риск — только чтение существующих данных.
- Нет изменений схемы.

---

### FT-20260623-007 — Инструменты шаблонов: песочница + импорт/экспорт + резервное копирование

Status: idea

Priority: low

Source: анализ спеки 2026-06-23 (разделы 4.4, 13)

Created: 2026-06-23

Context:

Три связанных функции из «Расширенного режима» спеки, которые повышают безопасность и удобство работы с шаблонами.

Proposed task:

1. **Песочница шаблонов** — запустить шаблон на выбранной теме без сохранения результата как текущей версии. Результат виден в отдельном окне.
2. **Импорт/экспорт шаблонов** — экспорт всего набора шаблонов в JSON-файл, импорт из файла с заменой или слиянием.
3. **Резервное копирование** — локальный экспорт всей SwiftData-базы в JSON/архив. Опциональный авто-бэкап по расписанию.

Acceptance criteria:

- Песочница не меняет ленту версий.
- Импорт не ломает существующие шаблоны (предупреждение о перезаписи).
- Экспорт/импорт резервной копии работает без потери данных.

Promotion notes:

- Реализовывать по одному пункту — не смешивать.
- Резервное копирование — наибольший риск (SwiftData export API); оценить перед началом.

---

### FT-20260623-008 — Расширенный режим (тумблер, скрывающий power-user функции)

Status: idea

Priority: low

Source: анализ спеки 2026-06-23 (раздел 4.5)

Created: 2026-06-23

Context:

Спека предусматривает тумблер «Показать расширенные функции» в Настройках. По умолчанию скрывает: параметры моделей в шаблонах (модель, температура, max tokens), полные логи с токенами и стоимостью, создание новых типов статей.

Proposed task:

1. Добавить `@AppStorage("advancedMode") var advancedMode: Bool = false` + тумблер в `SettingsView`.
2. В `TemplatesView` и других местах прятать power-user контролы за этим флагом.

Acceptance criteria:

- При выключенном режиме новичок не видит параметры моделей и сложные настройки.
- При включённом — всё доступно как сейчас.

Promotion notes:

- Чисто UI-изменение, нет изменений схемы.
- Не скрывать за флагом то, что нужно в ежедневной работе.

---

### FT-20260626-003 — Стабильная подпись приложения, чтобы Keychain не спрашивал пароль каждый раз

Status: idea

Priority: medium

Source: вопрос пользователя 2026-06-26 (промпт Keychain при каждом запуске/тестах)

Created: 2026-06-26

Context:

При каждой пересборке Xcode подписывает приложение заново (ad-hoc подпись со свежим хешем), поэтому macOS считает бинарник «новым» и снова просит пароль login-keychain для доступа к записи `SEOContentCreator.OpenAI` (см. `Logic/KeychainService.swift`). Кнопка «Always Allow» помогает только до следующей сборки. На тестах хуже: каждый прогон — новый бинарник. Продакшн-код уже изолирован от Keychain через `keyProvider`-замыкание (`StageExecutor`/`ImageGenerator`/`FragmentEditor`); реально в системный Keychain ходят только `KeychainServiceTests` (3 теста, тестируют саму обёртку) и само приложение.

Proposed task:

1. **Шаг 1 (основной, настройка в Xcode, делает пользователь):** target `SEOContentCreator` → Signing & Capabilities → выбрать Team (личный Apple ID / Personal Team) + Automatically manage signing. Стабильная подпись → ACL в Keychain не сбрасывается → одного «Always Allow» хватает надолго и для приложения, и для тестового раннера.
2. **Шаг 2 (опционально, правка кода):** добавить capability Keychain Sharing и читать/писать ключ с флагом `kSecUseDataProtectionKeychain` в `KeychainService` — доступ привязывается к Team ID, а не к бинарнику, промпт уходит совсем. Требует Team из шага 1. Изменение в работе с хранилищем — объяснить риски заранее.
3. **Шаг 3 (опционально, тесты):** гейт для `KeychainServiceTests` по переменной окружения (например `RUN_KEYCHAIN_TESTS=1`), чтобы в обычном Cmd+U эти 3 теста пропускались и не всплывал пароль.

Acceptance criteria:

- После шага 1 приложение и тесты перестают спрашивать пароль login-keychain при каждой сборке (после однократного «Always Allow»).

Promotion notes:

- Шаг 1 — это настройка в окне Xcode, не правка кода; код менять не нужно.
- НЕ открывать запись «для всех приложений» (`SecAccessCreate` с пустым списком доверия) — снижает безопасность ключа OpenAI; правильный путь — data-protection keychain (шаг 2).
- Шаги 2 и 3 — отдельные scope, не смешивать.
